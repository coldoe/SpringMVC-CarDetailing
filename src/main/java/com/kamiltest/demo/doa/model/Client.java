package com.kamiltest.demo.doa.model;import com.fasterxml.jackson.annotation.JsonBackReference;import org.springframework.lang.Nullable;import javax.persistence.*;import javax.validation.constraints.Max;import javax.validation.constraints.Min;import javax.validation.constraints.NotNull;@Entitypublic class Client {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long Id;    @NotNull(message = "Cant be null")    @Min(value = 2,message = "Too short, must be atleast 2 characters")    @Max(value = 255, message = "Too long, max size is 255 characters")    private String name;    @NotNull(message = "Cant be null")    @Min(value = 2,message = "Too short, must be atleast 2 characters")    @Max(value = 255, message = "Too long, max size is 255 characters")    private String surname;    @NotNull(message = "Cant be null")    @Min(value = 3,message = "Too short, must be atleast 3 characters")    @Max(value = 20, message = "Too long, max size is 20 characters")    private String contactNumber;    @OneToOne(cascade = CascadeType.ALL)    @JoinColumn(name = "car_id",referencedColumnName = "Id")    @Nullable    private Car car;//    @OneToOne(cascade = CascadeType.ALL)//    private Order order;    public Client() {    }    public Client(Long id, String name, String surname, String contactNumber, Car car, Order order) {        Id = id;        this.name = name;        this.surname = surname;        this.contactNumber = contactNumber;        this.car = car;//        this.order = order;    }    @Override    public String toString() {//        return "Client{" +//                "Id=" + Id +//                ", name='" + name + '\'' +//                ", surname='" + surname + '\'' +//                ", contactNumber='" + contactNumber + '\'' +//                ", car=" + car.getId() +//                '}';        return null;    }    //to prevent recursion//    @JsonManagedReference    @JsonBackReference    public Car getCar() {        return car;    }    public void setCar(Car car) {        this.car = car;    }    public Long getId() {        return Id;    }    public void setId(Long id) {        Id = id;    }    public String getName() {        return name;    }    public void setName(String name) {        this.name = name;    }    public String getSurname() {        return surname;    }    public void setSurname(String surname) {        this.surname = surname;    }    public String getContactNumber() {        return contactNumber;    }    public void setContactNumber(String contactNumber) {        this.contactNumber = contactNumber;    }}